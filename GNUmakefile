include GNUmakefile.std

# Indicate to my generic recusive "make" rules
# that we should finish making "build" in libroll
# before starting to make "build" in pct.

pct/build.before::	libroll/build.recurse

